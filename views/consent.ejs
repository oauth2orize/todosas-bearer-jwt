<!doctype html>
<html lang="en">
	<head>
		<meta charset="utf-8">
		<meta name="viewport" content="width=device-width, initial-scale=1">
		<title>Express • TodoMVC</title>
		<link rel="stylesheet" href="/css/base.css">
		<link rel="stylesheet" href="/css/index.css">
		<link rel="stylesheet" href="/css/login.css">
	</head>
	<body>
		<section class="prompt">
			<h3>todos</h3>
			<h2><%= client.name %> wants to access your account</h2>
			<% if (scope) { %>
			<p class="instructions">This will allow <%= client.name %> access to:</p>
			<ul class="scope">
				<% scope.forEach(function(value) { %>
				<li><%= value %></li>
				<% }); %>
			</ul>
			<% } %>
			<form action="<%= action %>" method="post">
				<input type="hidden" name="client_id" value="<%= client.id %>">
				<% if (scope) { %>
				<input type="hidden" name="scope" value="<%= scope.join(' ') %>">
				<% } %>
				<input type="hidden" name="_csrf" value="<%= csrfToken %>">
				<button type="submit">Allow</button>
			</form>
		</section>
		<footer class="info">
			<p>Created by <a href="https://www.jaredhanson.me">Jared Hanson</a></p>
			<p>Part of <a href="https://todomvc.com">TodoMVC</a></p>
			<p>Authentication powered by <a href="https://www.passportjs.org">Passport</a></p>
			<p>OAuth 2.0 powered by <a href="https://www.oauth2orize.org">OAuth2orize</a></p>
		</footer>
	</body>
</html>
